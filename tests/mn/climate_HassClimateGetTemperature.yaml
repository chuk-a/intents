language: mn
tests:
  # Add at least one test for each sentence in sentences/mn/climate_HassClimateGetTemperature.yaml
  # Example
  - sentences:
      - "Одоо температур хэд байна?"
    intent:
      name: HassClimateGetTemperature
    response: 21 °C
